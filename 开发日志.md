# 5hAgent 开发日志

> 用途：按时间线记录项目实现演进、重要取舍和验证结果。
> 文档分工：本文件记录“发生了什么”；`doc/` 下的模块文档记录“现在怎么实现”。

## 2026-06-23

### LLM 接口统一为 Claude / OpenAI-compatible

- 将运行时 provider 风格收敛为 `claude` 与 `openai`，模型名仍由 `LLM_<PROVIDER>_MODEL` 自由指定。
- 本地 Ollama 不再作为单独 provider 接入；通过 OpenAI-compatible `/v1` 接口使用，例如 `LLM_PROVIDER=openai`、`LLM_OPENAI_BASE_URL=http://localhost:11434/v1`。
- `internal/llm/client.go` 使用 Eino OpenAI adapter 创建 OpenAI-compatible chat model，保留 Claude adapter 处理 Claude/Anthropic 风格接口。
- `internal/utils/utils.go` 更新配置默认值、provider 校验和 `LLM_OPENAI_*` 读取规则，并通过测试覆盖拒绝旧 `ollama` provider 的行为。
- 文档、安装提示和模型 prefix prompt 示例同步改为 `LLM_OPENAI_*` / `prefix.openai...`。

### Headless 正常工作日志

- 新增无头模式 work log：`5hagent run` 默认在 stdout 显示任务头、assistant 流式文本、工具调用/结果/错误摘要和最终状态。
- 新增 `5hagent run --quiet`，脚本场景可压制 work log，只保留 report 路径和错误。
- `--debug` 仍表示更完整的 DEBUG 文件日志；work log 是正常运行可观察性，不等同于 debug。
- 实现位置：`internal/runtime/worklog.go`，通过 `logger.SetToolEventSink` 复用已有工具事件通道。

### Headless 工作日志项目内落盘

- 调整无头 work log 存储位置：每次 `5hagent run` 都写入项目目录 `.5hagent/agents/<agent-name>/logs/<timestamp>-<task-id>.md`。
- Agent 目录名来自 `AGENT_NAME` / `Agent.Name()`，经过 `safeName()` 处理；如果运行多个 Agent，会自然分散到多个目录。
- `--quiet` 语义收窄为只压制终端输出；项目内 Markdown 工作日志仍会写入，便于事后审计。
- 日志文件记录任务头、assistant 流式文本、工具事件摘要和最终状态；终端默认额外打印 `worklog: <path>`。

### 尝试: LLM 通过Tool Use 自主管理上下文

- 新增 `context.context` 工具，将部分上下文治理能力暴露给 LLM。
- 支持 action：
  - `inspect`：返回 message index、role、preview、protected/pinned flags。
  - `pin`：保护指定消息范围，并写入 audit event。
  - `audit`：返回最近上下文管理事件。
  - `compress`：允许 LLM 主动触发 `mode=lm` 或 `mode=truncate` 压缩。
- `internal/agent/agent.go` 在每轮 `RunStream` 添加用户消息后，通过 `agentctx.WithToolRuntime` 把当前 `Manager` 和 `Context` 放入 Go context，供 context 工具定位当前会话。
- `internal/runtime/runtime.go` 在本地工具注册后调用 `tools.RegisterContextTool(client.GetModel(), promptDir)`，并在 `WithTools` 前把该工具纳入 LLM 可见工具列表。
- `internal/toolmeta` 增加 `CategoryContext`，`internal/tools/registry.go` 增加 `RegisterContextTool`。

### 自动压缩开关与持久化修正

- 新增配置 `AGENT_CONTEXT_AUTO_COMPRESS`，默认 `true`。
- `Agent.Config` 和 `utils.AgentConfig` 增加 `ContextAutoCompress`。
- 自动压缩现在只有在 `AGENT_CONTEXT_AUTO_COMPRESS=true` 且 `ShouldCompress()` 命中时触发；设置为 `false` 后可用于测试 LLM 是否会主动调用 context 工具。
- `Manager.ReplaceMessages()` 和 `Store.ReplaceMessages()` 用于压缩后同步替换内存消息和 session JSONL，避免压缩只改内存、不改持久化文件。

### 冒烟验证

- 在测试 workspace `/Users/bytedance/Proj/5hWorkSpace` 中执行 headless 任务 `context-tool-self-manage-smoke`。
- 模型按顺序调用：
  - `base.read_file` 读取 `.5hagent/task.md`
  - `context.context {"action":"inspect"}`
  - `context.context {"action":"pin","start":1,"end":1,...}`
  - `context.context {"action":"audit"}`
  - `task.task {"action":"update","status":"completed"}`
- 报告写入 `.5hagent/reports/context-tool-self-manage-smoke.md`。
- 验证命令：
  - `go build -o 5hagent cmd/5hagent/main.go` 通过。
  - `go test ./internal/context ./internal/tools ./internal/agent ./internal/runtime` 通过。
  - `go test ./...` 仍有 `internal/cli` ANSI 渲染测试失败，和 context 工具链路无关。

## 2026-06-22

### LLM Provider 与工具调用链路

- 当时文档补齐 Claude/Ollama provider 配置，并通过 `LLM_PROVIDER` 选择当前模型；当前规则见 2026-06-23 的 OpenAI-compatible 接口统一记录。
- `LLM_<PROVIDER>_*` 配置可以同时保留，运行时只读取当前 provider 的配置。
- 补充 `doc/llm-call-flow.md`，说明 Eino `ToolCallingChatModel`、`schema.Message`、`schema.ToolCall`、5hAgent tool collector 与 tool result 回灌之间的边界。
- 为 Ollama/Qwen 类本地模型补充模型 prefix prompt 命名规则和工具调用注意事项。

### 流式 ToolCall 稳定性

- `toolCollector` 负责合并流式 tool call 分片。
- 对无 ID 的 tool call 补 `call_local_<n>`，保证 assistant tool call 和 tool result 能配对。
- 空参数工具不会在流式阶段过早补 `{}`；只有流结束后仍为空参数的完整调用才由 `PendingRunnableCalls()` 标准化。

## 2026-06-16

### Baseline 规整

当前 baseline 不再把 TUI 当成唯一入口，而是拆成三层：

1. `internal/runtime`：无头/TUI 共享运行时，负责初始化配置、LLM、Agent、工具、MCP、任务文件和报告目录。
2. `internal/agent` + `internal/tools` + `internal/task`：核心 ReAct、工具调用、文件任务状态。
3. `cmd/5hagent` + `internal/cli`：薄入口。默认启动 TUI；`5hagent run` 走无头任务执行。

### 文件交互约定

- 输入：项目目录 `.5hagent/task.md`。
- 输出：项目目录 `.5hagent/reports/<task-id>.md`。
- 状态：`pending -> in_progress -> completed`；失败写为 `failed`。
- 交互：先用文件记录任务和报告，不急于补完整 daemon/API。

### 当前保留能力

- ReAct 循环和流式工具调用。
- 基础文件工具和 shell 工具。
- Task/Skill/MCP 工具统一注册。
- TUI 仍保留，但变成 runtime 的一个前端。
- 仓库内 `testspace/` fixture 已移除；真实 headless/LLM/toolcall 测试统一放到 `/Users/bytedance/Proj/5hWorkSpace`。

### 待继续规整

- 控制代码量，减少 CLI/TUI 对核心 runtime 的侵入。
- 给 `internal/runtime` 补测试或 fake model 验证。
- 确认 `learn/stage-*` 分支是否补不可变 tag。
