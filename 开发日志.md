# 5hAgent 开发日志

> 用途：按时间线记录项目实现演进、重要取舍和验证结果。
> 文档分工：本文件记录“发生了什么”；`doc/` 下的模块文档记录“现在怎么实现”。

## 2026-06-26

### Stage 6 设计：Agent Systemd 顶层调度层

- 新增 `doc/agent-systemd.md`，把下一阶段定位为 AI 无关的顶层调度层，正式设计名为 Agent Systemd。
- 设计中将每个 Agent 视作独立进程：独立配置、独立上下文、独立 session、独立日志和生命周期。
- Agent Systemd 借鉴 OS / supervisor 的进程管理语义，但目标是 Agent 调度框架，不复刻完整操作系统。
- AgentProcess 启动参数收敛为 `PromptSpec` 和 `ExitSpec`；Prompt 只包含 system prompt 与已激活 skill 的 `Name/Description`。
- context 被视作进程内存，进程退出后销毁；session 持久化必须由 Agent 通过系统级工具显式创建。
- `decision` 类似受控升级点：只有纯规则无法判断字符串事实时才调用一次 LM API，并返回结构化 JSON；除该判断点外，调度组件保持写死的确定性逻辑。
- 当前 `internal/runtime` 被定义为执行引擎：Agent Systemd 可以在一个 OS 下启动多份 runtime，但后续实现前需要处理 `tools` registry、logger sink、工作目录等包级或进程级边界。
- 已新增 `internal/systemd` 最小调度链路：进程表、`ProcessSpec`、`SkillRefs` 提取已激活 skill 的 `Name/Description`、`StartProcess` 记录进程、`ListProcesses` 返回快照、`RunProcess` 通过外部 runner 同步执行单进程且用最小锁保护进程表、`ShouldExit` 基础退出判断；`Event.ProcessID` 用于限定目标进程，避免多进程事件误伤。
- 新增 `runtime.NewInMemory`，为 Agent Systemd 预留默认不绑定 session 的 Runtime 初始化路径；仍保留 session store，供 `sys.session.create/save` 显式持久化；现有 TUI/headless 默认行为保持不变。
- 新增 `Runtime.RunProcess` 适配器，让 runtime 满足 `systemd.ProcessRunner`；当前会先把 `ProcessSpec.Prompt` 注入内存 context，再调用一次 `Agent.RunStream`。
- Agent Systemd 进程模式明确采用“进程 prompt 替代默认 main prompt”；完整 skill 正文由 Agent 通过 `skill.skill get` 显式查看。
- 新增 `context.Manager.BindSession/SaveSession/DropSession`，作为 `sys.session.*` 系统工具的底座；默认内存 context 仍不会自动落盘。
- 新增 `sys.session` 工具，支持 `create/save/drop`，让 Agent 显式持久化或解除当前 context 的 session 绑定。
- 新增 `systemd.DecisionCaller` 和 `AgentSystemd.Decision`，只允许一次外部 caller 调用，并通过 `ParseDecision` 校验 JSON。
- `ParseDecision` 使用严格 JSON 解析，拒绝未知字段，避免 decision 输出夹带未声明命令或正文。
- 新增 `Runtime.CallDecision`，使用未绑定工具的原始模型执行一次 `Generate`，作为具体 `DecisionCaller` 适配器。
- 新增 `AgentSystemd.ApplyDecision`，当前执行 `start_agent/retry_agent`；`stop_agent` 需要 `target_process_id`，会 cancel 并把目标进程标记为 stopped；`wait/escalate` 暂不动作。
- 新增 `AgentSystemd` 内存 IPC 队列：`IPCMessage`、`Send`、`Recv`，只传短消息或 artifact 路径，不共享 context。
- `Runtime.RunProcess` 通过 `ToolRuntime` 注入当前 `ProcessID/IPC`，新增 `sys.ipc` 工具，支持 Agent 进程间发送/接收短消息或 artifact 路径。
- 新增 `tools.Registry` 结构体，并让现有包级工具注册函数先代理默认 registry。
- `Runtime` 已持有独立 `tools.Registry`，本地工具、`context.context` 和 MCP 工具都注册到当前 runtime registry；包级 registry 仍保留兼容入口。
- `tools.Registry` 的工具列表和 MCP server map 对外返回副本，MCP 注册路径加锁，降低多 Runtime 互相污染风险。
- `tools.Registry` 已持有自己的工具元数据 map；`internal/toolmeta` 收敛为类型和 fallback，不再维护包级可变 registry。
- 清理文档中不存在的 `mcp.list_tools` 描述；当前 MCP 工具以 `mcp.<server>.<tool>` 形式注册到 runtime registry。
- 新增 `runtime.Options.ProjectDir`，允许显式指定 AgentProcess 的 Project；任务文件、worklog、report 使用该 Project 下的 `.5hagent`。
- `agent.Config.ProjectDataDir` 让项目 skill 加载跟随 runtime 的 Project，不再强制依赖进程当前工作目录。
- `tools.Registry` 增加 workspace root，base 文件工具的相对路径和 `exec_shell` 执行目录跟随 runtime Project；绝对路径仍优先。
- 新增 `logger.PushToolEventSink`，TUI/headless worklog 使用保存/恢复语义，避免退出时无条件清空外层 sink。
- 新增 `AgentSystemd.Emit/Run/DispatchEvent` 最小事件循环，`Emit` 对非空事件 ID 去重并唤醒循环；`Run` 会等待事件直到 context 取消；`process.start/task.created/manual.request` 可从 payload 解析 `ProcessSpec` 并启动进程，`timer.tick` 会扫描 `Deadline/MaxTurns`，其他已有事件用于更新状态。
- 新增 `StartEvent` 辅助函数，把 `ProcessSpec` 编码成启动类事件 payload，减少外部调度入口重复拼 JSON。
- `timer.tick` 也会基于 `LastActiveAt` 做最小 stalled 检测，超过阈值的 running 进程会被 cancel 并标记为 stopped。
- 新增 `AgentSystemd.StartTimer`，可按固定间隔发出 `timer.tick`，作为最小内置事件源。
- 新增 `AgentSystemd.StartSource` 和 `EventSource`，为外部 watcher/manual source 提供最小接入接口；尚未实现具体文件 watcher。
- `Event.Risk=high` 时只记录事件，不启动进程，也不触发 decision。
- 新增 `AgentSystemd.RunWithDecision`，仅在 `task.failed` 事件后调用受控 decision；同一失败 key 默认最多触发一次，并用 `ApplyDecision` 执行返回动作。
- `DecisionInput.Policy` 收敛为结构体，当前暴露 `dedup_event_id`、`single_process`、`max_retries` 三条硬编码规则摘要。
- 修复 `sys.ipc` 发送方丢失问题，`SendIPC` 现在写入当前 `ProcessID`。
- `runtime.Options.ProjectDir` 在 Runtime 创建时固定为绝对 Project root，避免空值时后续随 cwd 漂移。
- `DispatchEvent` 对启动类事件异步调用 `RunProcess`，避免长运行进程阻塞事件循环。`Runtime.RunProcess` 采用“进程 prompt 替代默认 main prompt”的设计选择。
- `RunProcess` 结束后会自动投递 `process.exited` 或 `process.failed` 事件，失败事件 payload 只包含 runner 错误文本；report/worklog 收集仍留给 runtime 外层接入。
- `RunWithDecision` 调用 decision 时会传入当前事件、进程表快照和硬编码 policy 摘要，避免 LM 只看到失败事件而缺少调度上下文。
- 新增 `FileEventSource`，以最小轮询方式观察单个文件的 mtime/size，变化时产出 `file.changed` 或调用方指定事件；首次读取只建立基线，具体任务语义解析仍留给上层。
- `Runtime.RunProcess` 会为 AgentProcess 写入 Project 下的进程报告和 worklog，并把路径回填到 `AgentProcess`；Agent Systemd 的结束事件 payload 会携带这些 artifact 路径。
- `DecisionInput.Policy` 补齐 `high_risk_wait` 和 `stalled_after`，让 decision 看到当前已硬编码执行的风险和 stalled 策略。

## 2026-06-23

### LLM 接口统一为 Claude / OpenAI-compatible

- 将运行时 provider 风格收敛为 `claude` 与 `openai`，模型名仍由 `LLM_<PROVIDER>_MODEL` 自由指定。
- 本地 Ollama 不再作为单独 provider 接入；通过 OpenAI-compatible `/v1` 接口使用，例如 `LLM_PROVIDER=openai`、`LLM_OPENAI_BASE_URL=http://localhost:11434/v1`。
- `internal/llm/client.go` 使用 Eino OpenAI adapter 创建 OpenAI-compatible chat model，保留 Claude adapter 处理 Claude/Anthropic 风格接口。
- `internal/utils/utils.go` 更新配置默认值、provider 校验和 `LLM_OPENAI_*` 读取规则，并通过测试覆盖拒绝旧 `ollama` provider 的行为。
- 文档、安装提示和模型 prefix prompt 示例同步改为 `LLM_OPENAI_*` / `prefix.openai...`。

### Headless 正常工作日志

- 新增无头模式 work log：`5hagent run` 默认在 stdout 显示任务头、assistant 流式文本、工具调用/结果/错误摘要和最终状态。
- 新增 `5hagent run --quiet`，脚本场景可压制 work log，只保留 report 路径和错误。
- `--debug` 仍表示更完整的 DEBUG 文件日志；work log 是正常运行可观察性，不等同于 debug。
- 实现位置：`internal/runtime/worklog.go`，通过 `logger.SetToolEventSink` 复用已有工具事件通道。

### Headless 工作日志项目内落盘

- 调整无头 work log 存储位置：每次 `5hagent run` 都写入项目目录 `.5hagent/agents/<agent-name>/logs/<timestamp>-<task-id>.md`。
- Agent 目录名来自 `AGENT_NAME` / `Agent.Name()`，经过 `safeName()` 处理；如果运行多个 Agent，会自然分散到多个目录。
- `--quiet` 语义收窄为只压制终端输出；项目内 Markdown 工作日志仍会写入，便于事后审计。
- 日志文件记录任务头、assistant 流式文本、工具事件摘要和最终状态；终端默认额外打印 `worklog: <path>`。

### 尝试: LLM 通过Tool Use 自主管理上下文

- 新增 `context.context` 工具，将部分上下文治理能力暴露给 LLM。
- 支持 action：
  - `inspect`：返回 message index、role、preview、protected/pinned flags。
  - `pin`：保护指定消息范围，并写入 audit event。
  - `audit`：返回最近上下文管理事件。
  - `compress`：允许 LLM 主动触发 `mode=lm` 或 `mode=truncate` 压缩。
- `internal/agent/agent.go` 在每轮 `RunStream` 添加用户消息后，通过 `agentctx.WithToolRuntime` 把当前 `Manager` 和 `Context` 放入 Go context，供 context 工具定位当前会话。
- `internal/runtime/runtime.go` 在本地工具注册后调用 `tools.RegisterContextTool(client.GetModel(), promptDir)`，并在 `WithTools` 前把该工具纳入 LLM 可见工具列表。
- `internal/toolmeta` 增加 `CategoryContext`，`internal/tools/registry.go` 增加 `RegisterContextTool`。

### 自动压缩开关与持久化修正

- 新增配置 `AGENT_CONTEXT_AUTO_COMPRESS`，默认 `true`。
- `Agent.Config` 和 `utils.AgentConfig` 增加 `ContextAutoCompress`。
- 自动压缩现在只有在 `AGENT_CONTEXT_AUTO_COMPRESS=true` 且 `ShouldCompress()` 命中时触发；设置为 `false` 后可用于测试 LLM 是否会主动调用 context 工具。
- `Manager.ReplaceMessages()` 和 `Store.ReplaceMessages()` 用于压缩后同步替换内存消息和 session JSONL，避免压缩只改内存、不改持久化文件。

### 冒烟验证

- 在测试 workspace `/Users/bytedance/Proj/5hWorkSpace` 中执行 headless 任务 `context-tool-self-manage-smoke`。
- 模型按顺序调用：
  - `base.read_file` 读取 `.5hagent/task.md`
  - `context.context {"action":"inspect"}`
  - `context.context {"action":"pin","start":1,"end":1,...}`
  - `context.context {"action":"audit"}`
  - `task.task {"action":"update","status":"completed"}`
- 报告写入 `.5hagent/reports/context-tool-self-manage-smoke.md`。
- 验证命令：
  - `go build -o 5hagent cmd/5hagent/main.go` 通过。
  - `go test ./internal/context ./internal/tools ./internal/agent ./internal/runtime` 通过。
  - `go test ./...` 仍有 `internal/cli` ANSI 渲染测试失败，和 context 工具链路无关。

## 2026-06-22

### LLM Provider 与工具调用链路

- 当时文档补齐 Claude/Ollama provider 配置，并通过 `LLM_PROVIDER` 选择当前模型；当前规则见 2026-06-23 的 OpenAI-compatible 接口统一记录。
- `LLM_<PROVIDER>_*` 配置可以同时保留，运行时只读取当前 provider 的配置。
- 补充 `doc/llm-call-flow.md`，说明 Eino `ToolCallingChatModel`、`schema.Message`、`schema.ToolCall`、5hAgent tool collector 与 tool result 回灌之间的边界。
- 为 Ollama/Qwen 类本地模型补充模型 prefix prompt 命名规则和工具调用注意事项。

### 流式 ToolCall 稳定性

- `toolCollector` 负责合并流式 tool call 分片。
- 对无 ID 的 tool call 补 `call_local_<n>`，保证 assistant tool call 和 tool result 能配对。
- 空参数工具不会在流式阶段过早补 `{}`；只有流结束后仍为空参数的完整调用才由 `PendingRunnableCalls()` 标准化。

## 2026-06-16

### Baseline 规整

当前 baseline 不再把 TUI 当成唯一入口，而是拆成三层：

1. `internal/runtime`：无头/TUI 共享运行时，负责初始化配置、LLM、Agent、工具、MCP、任务文件和报告目录。
2. `internal/agent` + `internal/tools` + `internal/task`：核心 ReAct、工具调用、文件任务状态。
3. `cmd/5hagent` + `internal/cli`：薄入口。默认启动 TUI；`5hagent run` 走无头任务执行。

### 文件交互约定

- 输入：项目目录 `.5hagent/task.md`。
- 输出：项目目录 `.5hagent/reports/<task-id>.md`。
- 状态：`pending -> in_progress -> completed`；失败写为 `failed`。
- 交互：先用文件记录任务和报告，不急于补完整 daemon/API。

### 当前保留能力

- ReAct 循环和流式工具调用。
- 基础文件工具和 shell 工具。
- Task/Skill/MCP 工具统一注册。
- TUI 仍保留，但变成 runtime 的一个前端。
- 仓库内 `testspace/` fixture 已移除；真实 headless/LLM/toolcall 测试统一放到 `/Users/bytedance/Proj/5hWorkSpace`。

### 待继续规整

- 控制代码量，减少 CLI/TUI 对核心 runtime 的侵入。
- 给 `internal/runtime` 补测试或 fake model 验证。
- 确认 `learn/stage-*` 分支是否补不可变 tag。
