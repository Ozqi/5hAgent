%% 事实源: .doc/walle-architecture-topology.json view_overall_runtime
flowchart LR
  cli["cmd/walle"]
  tui["internal/tui"]
  daemon["walle daemon\ninteractive host"]
  socket["supervisor.sock"]
  runtime["internal/runtime"]
  agent["internal/agent"]
  context["internal/context.Manager"]
  llm["internal/llm + codex"]
  registry["internal/tools.Registry"]
  sessions["sessions/*.jsonl"]

  cli -->|spawn if missing| daemon
  cli -->|attach| socket
  cli -->|launch| tui
  tui <-->|input / events| socket
  daemon -->|create| runtime
  runtime --> agent
  runtime --> context
  runtime --> llm
  runtime --> registry
  context --> sessions
