# walle

Claude Code 进入仓库后先阅读 [AGENTS.md](AGENTS.md)，它是当前完整工作契约。

执行顺序：

1. 修改代码前读取对应 `.spec/*.md`，再核对当前实现。
2. 开发计划和设计写入 `.TODO/`。
3. 架构图和拓扑事实源写入 `.spec/diagrams/`。
4. 内部调研和决策写入 `.doc/`，`doc/` 只发布已实现能力和用户使用方法。
5. 优先复用现有结构，保持最小改动；Go 代码执行 `gofmt`。
6. 默认不运行测试、不提交、不推送；阶段收尾或用户明确要求时再验收。

当前项目名和 GitHub 仓库名为 `walle`。兼容接口仍使用 `walle`、`~/.walle` 和 `.walle`。
