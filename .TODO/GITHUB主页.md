# 更新 README

- 项目名和 GitHub 仓库名统一为 `walle`，仓库地址使用 `Ozqi/walle`。
- 保留 `walle` CLI 和现有数据目录，避免破坏已有安装。
- README 使用文字标题，终端图标以 `internal/tui/app.go` 为真源。
- 展示“Go 实现、轻量极简、充分使用工具、可验证自修改”的特点。
- 补充功能截图和 GIF（人工制作）。

## 发布前

- [ ] 人工 review 后在 GitHub 将 `Ozqi/walle` 重命名为 `Ozqi/walle`。
- [ ] 重命名后确认 README 安装链接和本地 `origin` 可访问。
