# read我希望做的更复杂

## 对于md文件
应该有更多高效的能力。

递归只读取所有标题
读取指定分级标题下的内容。

剪切某标题
复制、删除某标题下的内容
删除某标题下的内容


## think
这些可以不可以又Agent自己创建，持久的放到一个自动复用的工具区， 和 tool use一样平行。

## 待实现

- [x] `base.read_md` 支持标题列表、section 读取、替换和删除。实现：`3dbec5719e1930e4a49e61539b66c46991cfc5ff`。
- 剪切/复制可复用“读取 section + 写入 section”，但要先定义写操作的路径和覆盖安全边界。
- Agent 自己持久创建工具仍归动态工具注册设计，必须有脚本目录、权限边界、`--help`/schema 和人工审查；不能让 LLM 生成后立即进入默认工具列表。

## 完成记录

- 按标题替换/删除 Markdown section：`3dbec57b8b09fb98e589a1321ff9c27906819963`
- 修正 `base.read_md` 的可写元数据：`cd4197f256463b63606a514b5a98884ee43ca354`
